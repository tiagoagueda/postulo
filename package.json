{
  "name": "postulo-assets",
  "private": true,
  "description": "Stylesheet build for Postulo. Node is only needed to change the CSS; the compiled result is committed so the application runs without it.",
  "scripts": {
    "build:css": "tailwindcss --input assets/css/app.css --output src/postulo/static/css/app.css --minify",
    "watch:css": "tailwindcss --input assets/css/app.css --output src/postulo/static/css/app.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
